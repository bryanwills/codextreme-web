---
// Efecto de nebulosa CSS puro - Muy performante
---

<div id="nebula-background" class="fixed inset-0 pointer-events-none z-0 overflow-hidden">
  <div class="nebula-container">
    <div class="nebula-layer nebula-1"></div>
    <div class="nebula-layer nebula-2"></div>
    <div class="nebula-layer nebula-3"></div>
    <div class="stars-layer"></div>
  </div>
</div>

<style>
  .nebula-container {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }

  .nebula-layer {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.4;
    animation: nebula-drift 60s linear infinite;
  }

  .nebula-1 {
    background: radial-gradient(circle, 
      rgba(107, 91, 149, 0.8) 0%, 
      rgba(107, 91, 149, 0.5) 30%, 
      transparent 70%);
    animation-duration: 80s;
    animation-direction: normal;
  }

  .nebula-2 {
    background: radial-gradient(circle, 
      rgba(166, 74, 201, 0.7) 0%, 
      rgba(166, 74, 201, 0.4) 40%, 
      transparent 70%);
    animation-duration: 100s;
    animation-direction: reverse;
    animation-delay: -20s;
  }

  .nebula-3 {
    background: radial-gradient(circle, 
      rgba(138, 124, 168, 0.7) 0%, 
      rgba(138, 124, 168, 0.35) 35%, 
      transparent 65%);
    animation-duration: 120s;
    animation-direction: normal;
    animation-delay: -40s;
  }

  .stars-layer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      radial-gradient(2px 2px at 20px 30px, rgba(255,255,255,0.6), transparent),
      radial-gradient(2px 2px at 40px 70px, rgba(166, 74, 201, 0.7), transparent),
      radial-gradient(1px 1px at 90px 40px, rgba(107, 91, 149, 0.8), transparent),
      radial-gradient(1px 1px at 130px 80px, rgba(255,255,255,0.5), transparent),
      radial-gradient(2px 2px at 160px 30px, rgba(138, 124, 168, 0.7), transparent);
    background-repeat: repeat;
    background-size: 200px 100px;
    animation: stars-twinkle 20s linear infinite;
  }

  @keyframes nebula-drift {
    0% {
      transform: rotate(0deg) translateX(0px) translateY(0px);
    }
    33% {
      transform: rotate(120deg) translateX(50px) translateY(30px);
    }
    66% {
      transform: rotate(240deg) translateX(-30px) translateY(50px);
    }
    100% {
      transform: rotate(360deg) translateX(0px) translateY(0px);
    }
  }

  @keyframes stars-twinkle {
    0% {
      transform: translateX(0px) translateY(0px);
      opacity: 0.9;
    }
    50% {
      transform: translateX(20px) translateY(10px);
      opacity: 1;
    }
    100% {
      transform: translateX(40px) translateY(20px);
      opacity: 0.9;
    }
  }

  /* Efecto hover sutil en toda la página */
  body:hover .nebula-layer {
    opacity: 0.15;
    transition: opacity 2s ease;
  }

  /* Optimización para rendimiento */
  .nebula-layer {
    will-change: transform;
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  .stars-layer {
    will-change: transform, opacity;
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  /* Reducir efectos en dispositivos móviles */
  @media (max-width: 768px) {
    .nebula-layer {
      filter: blur(50px);
      opacity: 0.05;
    }
    
    .stars-layer {
      opacity: 0.6;
    }
  }

  /* Respetar preferencias de animación reducida */
  @media (prefers-reduced-motion: reduce) {
    .nebula-layer,
    .stars-layer {
      animation: none;
    }
    
    .nebula-layer {
      opacity: 0.05;
    }
  }

  /* Modo oscuro mejorado */
  @media (prefers-color-scheme: dark) {
    .nebula-layer {
      opacity: 0.15;
    }
    
    .stars-layer {
      opacity: 0.9;
    }
  }
</style>
